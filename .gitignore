node_modules/
dist/
.vite/
*.log
.DS_Store
/refs
# .claude is transient (worktrees, local settings) EXCEPT the checked-in
# project settings + hooks, which wire the AGENTS.md intent nodes into
# Claude Code's per-directory lazy loading.
.claude/*
!.claude/settings.json
!.claude/hooks/
