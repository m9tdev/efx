node_modules/
dist/
.vite/
*.log
.DS_Store
/refs
# .claude is transient (worktrees, local settings) EXCEPT rules/, which maps
# the AGENTS.md intent nodes into Claude Code's lazy per-directory loading.
.claude/*
!.claude/rules/
