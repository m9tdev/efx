---
paths:
  - "packages/core/src/testing/**"
---

INTENT-NODE core-testing: before reading or changing code under `packages/core/src/testing/`, Read `packages/core/src/testing/AGENTS.md` once this session — it holds the invariants and anti-patterns for that area.
