---
paths:
  - "packages/ts-plugin/**"
---

INTENT-NODE ts-plugin: before reading or changing code under `packages/ts-plugin/`, Read `packages/ts-plugin/AGENTS.md` once this session — it holds the invariants and anti-patterns for that area.
