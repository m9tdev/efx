---
paths:
  - "packages/core/src/compiler/**"
---

INTENT-NODE core-compiler: before reading or changing code under `packages/core/src/compiler/`, Read `packages/core/src/compiler/AGENTS.md` once this session — it holds the invariants and anti-patterns for that area.
