---
paths:
  - "scripts/**"
---

INTENT-NODE scripts: before reading or changing code under `scripts/`, Read `scripts/AGENTS.md` once this session — it holds the invariants and anti-patterns for that area.
