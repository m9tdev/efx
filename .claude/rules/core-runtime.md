---
paths:
  - "packages/core/src/runtime/**"
---

INTENT-NODE core-runtime: before reading or changing code under `packages/core/src/runtime/`, Read `packages/core/src/runtime/AGENTS.md` once this session — it holds the invariants and anti-patterns for that area.
