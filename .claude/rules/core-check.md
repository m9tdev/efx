---
paths:
  - "packages/core/src/check/**"
---

INTENT-NODE core-check: before reading or changing code under `packages/core/src/check/`, Read `packages/core/src/check/AGENTS.md` once this session — it holds the invariants and anti-patterns for that area.
