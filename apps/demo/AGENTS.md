# `apps/demo` — usage patterns by primitive

Each `.vx` file exercises a different framework primitive. The
demo is also the canonical "example" — when documenting an
intended pattern, prefer extending an existing demo over inventing
a new one elsewhere.

The actual app at `src/main.vx` mounts every demo into one page so
they can be exercised in a browser side-by-side.

## What each demo proves

| File               | Primitive                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Counter.vx`       | Local `Atom.make` state, no Effect services. The atom is dropped straight into child position (`{count}`); the handler returns the `Atom.update` Effect (a registry write, so R = `AtomRegistry`, discharged by `mount`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `UserPage.vx`      | In-component fetch: a `Component.make` body that `yield*`s `http.getUser` directly, so the subtree blocks until it resolves and `E = HttpError` + `R = Http \| Theme` fold to the root (root-handled failure). The channel-propagation example.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `AsyncUserPage.vx` | The same fetch as an `atom`, once form: `const user = yield* atom(http.getUser(id))` → `Atom<AsyncResult<User, HttpError>>`, rendered via `{AsyncResult.builder(get(user)).onInitialOrWaiting(…).onSuccess(…).onFailure(…).exhaustive()}` (a `get(...)` reader — no `Atom.map` layer). Non-blocking (waiting placeholder) and failure rendered as a value at the leaf, so `E = never`; `Http` still folds (the atom runs on the component's context) plus `atom`'s own `AtomRegistry \| Scope` → `Effect<View, never, Http \| AtomRegistry \| Scope>`. The leaf counterpart to `UserPage` — same data, opposite `E`. Renders into `.async-user-page` (distinct from UserPage's `.user-page`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `LiveUser.vx`      | Rendered with `<MatchTags on={user} Waiting Success Failure={(f) => …} />` (function `Failure` = handled at the leaf → `View<never>`). `atom` with a dependency: `atom((get) => http.getUser(get(userId)))` over an `Atom.make("42")` cell the buttons `Atom.set`. `get(userId)` in the create fn makes the id a tracked dep, so the fetch re-runs on change (interrupting the stale run) — no explicit trigger declared. Service extracted up front → `Effect<View, never, Http \| AtomRegistry \| Scope>` (Http folded), unlike a baked `Atom.runtime` which would discharge it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `Todos.vx`         | Keyed reactive list: `<For each={coll}>{(item) => <Row item={item}/>}</For>` over an `AtomRef.Collection` (rows are the item refs). Per-row reactivity: the row's JSX reads `get(item).done` / `item.prop("text")`, so toggling one row doesn't tear down others.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `Lifecycle.vx`     | `Effect.acquireRelease` inside a `<For>` row — release fires when the row is removed (per-row Scope in mount).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `CatchDemo.vx`     | The `Catch` error boundary (step 07), both forms: a function handler (catch-all over a `Crasher` whose click fails) and an object tag-map (`{ HttpError }` over a flaky `BadRequest` that ~50% of the time fails construction with a random typed error, routed + unwrapped — `retry` re-rolls and may recover). Discharges to `Effect<View, never, Scope>`; `reset`/`retry` re-run construction. Exercised end-to-end by `scripts/probe-catch.mjs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `AsyncEscalate.vx` | **Tag block** uses `<MatchTags on={tagUser} Waiting Success Failure={{ HttpError: … }} />` — `HttpError` handled at the leaf, `RateLimited` BUBBLES by default (residual `View<RateLimited>`). Failure homes over **`atom`s** (step 08): each block's data is an `Atom<AsyncResult<User, HttpError \| RateLimited>>` created in the component body — data outlives the views (an escalated block keeps tracking `userId` via `get(userId)`), spam is `Atom.refresh(tagUser)` ×3 (with a `yieldNow` between them — a handler's synchronous prefix is batched, so three back-to-back refreshes would coalesce into one recompute), and the banners' retries compose `Effect.andThen(Atom.refresh(x), Effect.sync(reset))` (`reset` alone re-renders CURRENT state without refetching). Two live blocks share one `userId` atom. **Tag block**: the fetch fails with `HttpError \| RateLimited` (a demo-local second tag); `HttpError` is handled **at the leaf** with `.onErrorTag("HttpError", …)` — the atom stays live (another id recovers with no boundary involved; the banner's `retry` is `Atom.refresh(tagUser)`, showing the waiting arm while in flight); `.onFailure(Effect.failCause)` escalates the residual (`View<RateLimited>` — the builder narrows `E` like `catchTag`) to a page-level `Catch` **tag map** (`.page-fallback`) that gets it unwrapped (`e.retryAfterMs` typed). Deleting that `Catch` names only `RateLimited`. The rate limit is **real**: a sliding-window limiter (2 req/s shared by both atoms) in `fetchUser`'s synchronous prefix — request-creation time (the atom forks its fiber synchronously), so every refetch counts deterministically, interrupted or not; the "spam ×3" button fires three refreshes back-to-back — enough to exceed the budget even when only one block is still live. A live **request log** (`.req-log`, an `Atom.make("")` cell written with `Atom.update` from inside the fetch) narrates every request: sent (n/2), ← 200/404, ← interrupted (stale run), → 429 — the limiter's whole story is on-screen. **Open block**: the same data with nothing handled at the leaf — every failure rides `View<HttpError \| RateLimited>` to a catch-all `Catch` (`.open-fallback`, `Cause.pretty` + `reset`); deleting it names the full union. The id buttons live outside both boundaries and survive every swap. Exercised end-to-end by `scripts/probe-escalate.mjs`. |
| `TypedHandlers.vx` | Typed event handlers (step 09, #72): both channels enter through `onclick` alone — the component body never yields a service. The inner `Loader` infers `Effect<View<HttpError>, never, Http \| AtomRegistry>` (the handler's 404 rides the LIVE channel; its `Http` and its `Atom.set` write fold into R); a tag-map `Catch` discharges the HttpError (`reset` rebuilds the card fresh) and the export is `Effect<View, never, Http \| AtomRegistry \| Scope>`. Deleting the `Catch` is a compile error naming HttpError; dropping `HttpLive` from APP_LAYER names Http.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SaveButton.vx`    | Pending → run → settle (step 10) as **`fn`**: `const save = yield* fn((id) => …)`; the handler returns `save(id)` (a registry write), and `{AsyncResult.builder(get(save)).onWaiting(spinner).onFailure(Effect.failCause).orElse(buttons)}` renders the call's state — waiting swaps the clicked button for a spinner, success settles back (`saved` += 1 via `Atom.update` inside the body), and a failure is escalated so the typed `HttpError` rides the live channel to the `Catch`. The fn is mounted on the component's Scope, so ↻ reset mid-flight interrupts the in-flight call (no stale write in the fresh instance). Same discharged shape as step 09. (The #160/#161 handler-scope semantics — a dispatch outliving the element that fired it — stay pinned by the runtime tests; this demo no longer relies on them.) Browser pin: `scripts/probe-savebutton.mjs`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `Clock.vx`         | `atom(stream)` (step 11) — push-based fine-grained updates from an Effect `Stream`: an analog clock whose tick stream (`Stream.tick("1 second")` mapped to `new Date()` — emits immediately, then every second) drives an `Atom<AsyncResult<Date>>` — no manual fork, no unsubscribe bookkeeping: the fiber dies with the component's `Scope` (unmount/reset interrupts it), and a service-needing stream would fold its `R` into the component. The hand angles are _derived_ (`Atom.map(date, …)`, equality-deduped by the registry: recomputes every tick, notifies only when its value changed), and each hand's `style` is a `get(...)` reader over exactly its own derived atom → one DOM write per tick (the second hand's rotation; minute/hour join at the top of the minute). The hands carry only `transform: rotate(…)` inline and deliberately no CSS transition, so the flash overlay (which observes `class` + `style` attribute mutations) marks exactly the write. `Effect<View, never, AtomRegistry \| Scope>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `main.vx`          | Wires Layers (`HttpLive`, `ThemeLive`) + `Effect.scoped` + `Effect.never` (keep scope alive for page lifetime).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `services.ts`      | Mock Http + Theme services. `Data.TaggedError` for `HttpError`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## No sibling `.ts` files on disk

Cross-file `.vx` imports carry an explicit `.vx` extension (see
the root [AGENTS.md](../../AGENTS.md) invariant). As a result
nothing in this demo's build pipeline emits sibling `.ts` files:
`pnpm typecheck` runs [`@verrex/core/check`](../../packages/core/src/check/AGENTS.md)
directly, `vite build` runs
[`@verrex/core/vite`](../../packages/core/src/vite-plugin/AGENTS.md)
directly, and the editor's
[TS plugin](../../packages/ts-plugin/AGENTS.md) maps virtual-code
results back to source `.vx` positions natively.

## `channels.test-d.ts`

Type-only test file. Uses `expectTypeOf` from `vitest` to assert
that:

- A `<Component />` whose body needs `Http` propagates `Http` into
  `R` at the surrounding `h()` call site (which is what the
  compiler turns the JSX-syntax into — see root
  [AGENTS.md](../../AGENTS.md)).
- A `<div>` containing such a component propagates `Http` too.
- Conditional / `&&` / list children all fold `E`/`R` correctly.
- A consumer that forgets a required Layer fails to type-check.

This is the **compile-time proof that channels survive the tree**.
If you touch `h()`'s signature, any of the `Fold*` types, or `View`
IR, run:

```
pnpm --filter verrex-demo typecheck
```

A regression here is the framework's central thesis breaking. The
file is intentionally not run as a runtime test — `expectTypeOf` is
purely a typing assertion.

## Vite dev server

`vite.config.ts` is intentionally minimal — it wires
`@verrex/core/vite`, which owns the whole `.vx` story (transform + URL
middleware; see [its node](../../packages/core/src/vite-plugin/AGENTS.md)).
Don't add a separate filename-rewrite shim; the plugin already handles it.

The dev server binds to `0.0.0.0` (set in vite.config.ts) so LAN
devices can hit the demo. Don't drop that without checking.

## Anti-patterns

- Don't import from `verrex/internal` — there is no internal
  subpath; everything is re-exported from the index.
- Don't wrap reactive reads in `.pipe(Effect.map(...))` for the
  common case — pass the atom/ref as a child or prop, or use
  `get(...)` in the expression (the compiler emits `h.reader`).
  Use Effect's combinators only when you actually need to compose
  multiple Effects.
- Don't put `get(...)` inside a nested function within a JSX
  expression (`{xs.map((x) => get(x).name)}`) or inside an `on*`
  handler — both are compile errors. Move the read to the
  expression level, use `<For>` for lists, or read imperatively
  in the handler (`registry.get` / `Atom.get`).
- Don't `.value` an `AtomRef` inside JSX and expect tracking —
  `.value` is plain member access now. `AtomRef` is the row model
  (`<For>` over a Collection); everything else is `Atom.make`.
- Don't add helper components that wrap intrinsics for no reason.
  The demo is opinionated about _not_ having a component library;
  it shows what falls out of the framework directly.
- Don't reach for `AtomRef.unsafeMake` or `Effect.runSyncUnsafe`
  in component code — the runtime expects everything reactive to
  flow through scoped effects.

## Related context

- Runtime contract: [`packages/core/src/runtime/`](../../packages/core/src/runtime/AGENTS.md)
- Compiler rewrites (what `.vx` actually becomes): [`packages/core/src/compiler/`](../../packages/core/src/compiler/AGENTS.md)
- Framework rationale + channel-fold thesis: root [`AGENTS.md`](../../AGENTS.md)
